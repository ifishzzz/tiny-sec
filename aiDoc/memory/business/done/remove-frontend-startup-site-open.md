# 去掉前端启动时自动打开官网

## 基本信息

- 完成日期：2026-05-12
- 当前状态：`done`
- 需求类型：前端启动行为调整

## 用户原始意图摘要

运行前端时不要自动打开 `https://www.gin-vue-admin.com/`。

## 本次实现

- 移除 `web/package.json` 中 `dev` 与 `serve` 启动脚本里的 `node openDocument.js`
- 保留 `Vite` 开发服务启动逻辑，不影响正常本地开发

## 影响范围

- 前端本地开发启动流程
- 不影响构建、预览和页面运行逻辑
